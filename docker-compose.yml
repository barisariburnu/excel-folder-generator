services:
  app:
    build: .
    container_name: excel-folder-generator
    ports:
      - "2323:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - uploads:/app/uploads
    restart: unless-stopped

volumes:
  uploads:
